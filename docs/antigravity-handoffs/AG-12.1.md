# AG-12.1 Handoff Document

## Status
READY_FOR_CODEX_REVIEW

## 변경 파일
- `src/app/ledger/page.tsx`: 
  - 기준정보(회사, 현장, 비용 분류)를 서버 `/api/master-data/` 경로에서 불러오고 선택할 수 있는 드롭다운 추가
  - '저장' 버튼 추가 및 클릭 시 선택한 기준정보와 입력된 원장 내역 중 유효한 내역(발생일과 품명이 있는 행)을 필터링하여 `POST /api/ledger`로 전송
  - 저장 전 클라이언트 측에서 `calculateMoney`를 이용해 한 번 더 공급가액과 부가세를 정확히 계산(서버 중앙 금액 계산 로직 재사용)하여 전송하도록 적용

## 실제 실행 명령 및 결과
- `npm run format:check`: 통과
- `npm run lint`: 통과
- `npm run typecheck`: 통과
- `npm run test`: 통과 (12 tests passed)
- `npm run build`: 통과 (정적 페이지 및 동적 라우트 정상 생성)

## 미실행 테스트
- E2E 테스트 혹은 브라우저 상의 직접 렌더링/인터랙션 테스트는 CLI 상에서만 수행하여 검증하지 않음.
- 세션 기반 실제 로그인 후 API 호출 시 인증/인가 통과 여부는 확인하지 않음.

## 미확인 업무 규칙
- 권한 검사, 세금 상세 규칙, 결재 및 지급 규칙에 대한 비즈니스 로직은 서버 단의 명세와 기존 `calculateMoney`에 전적으로 의존하며, 프론트엔드에서 임의로 추가하지 않음.
- 품명 중복, 수량/단가 한도 등에 대한 검사는 수행하지 않음.
